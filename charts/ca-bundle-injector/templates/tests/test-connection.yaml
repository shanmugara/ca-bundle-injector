apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ca-bundle-injector.fullname" . }}-test-connection"
  labels:
    {{- include "ca-bundle-injector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ca-bundle-injector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
